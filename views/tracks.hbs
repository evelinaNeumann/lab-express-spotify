<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Tracks</title>
  </head>
  <body>
    <h1>Tracks</h1>
    <ul>
      {{#each tracks}}
      <li>
        <audio controls>
          <source src="{{preview_url}}" type="audio/mp3">
          Your browser does not support the audio element.
        </audio>
        <p>{{name}}</p>
      </li>
      {{/each}}
    </ul>
  </body>
</html>
